# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native projects generated by `expo prebuild` (CI regenerates these fresh every run)
android/
ios/

# Debug keystore (kept out of git — stored as a GitHub secret instead, see README)
debug.keystore
*.keystore
*.jks

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS
.DS_Store

# Env
.env
.env.local
